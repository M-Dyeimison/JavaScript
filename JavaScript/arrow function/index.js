const doubleVelocity = (velocity) => {
    return velocity * 2
}

//only one line of code
const tripleVelocity = (velocity) => velocity * 3

//only one parameter
const quadrupleVelocity = velocity => velocity * 4

console.log(doubleVelocity(100))
console.log(tripleVelocity(100))
console.log(quadrupleVelocity(100))